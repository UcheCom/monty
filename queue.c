#include "monty.h"

/**
 * queue - this sets the format of the data to a queue
 * @head: double pointer to the head of the queue
 * @line: current line number
 *
 * Return: void
 */
void queue(stack_t **head, unsigned int line)
{
	(void)head;
	(void)line;

	uvar.qs_flag = 0;
}
